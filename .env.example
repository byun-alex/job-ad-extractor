# Copy to .env and fill ONE of these to run against a real model.
# With neither set, the harness uses the offline MockLLM so it still runs.

# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-haiku-4-5

# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
